# Library server (different server) - copy to .env and fill values
# Used when deploying at library.jamiaarabiaislamia.com on a separate host.

# Image source (e.g. GitHub Container Registry)
REGISTRY=ghcr.io
IMAGE_NAME_LOWER=your-org/your-repo

# Postgres (used by library-api)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password
LIBRARY_DB_NAME=library_db

# JWT (library API auth)
JWT_SECRET=your-jwt-secret
JWT_REFRESH_SECRET=your-jwt-refresh-secret
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# Cookie domain for auth (must match parent domain)
COOKIE_DOMAIN=.jamiaarabiaislamia.com

# CORS - allow library frontend origin
CORS_ORIGIN=https://library.jamiaarabiaislamia.com

# Caddy ports (optional; defaults 80/443)
HTTP_PORT=80
HTTPS_PORT=443

# Library app was built with this URL (set in CI build-args; only needed if you override at runtime)
# NEXT_PUBLIC_LIBRARY_API_URL=https://api.library.jamiaarabiaislamia.com

# R2 (for library database backups - optional; required for backup feature)
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=arabiaislamia
