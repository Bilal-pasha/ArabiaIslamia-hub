services:
  postgres:
    image: postgres:16-alpine
    container_name: arabiaaislamia-postgres-dev
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
      - ./init-databases.sql:/docker-entrypoint-initdb.d/init-databases.sql
    networks:
      - app-network-dev
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres" ]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: arabiaaislamia-pgadmin-dev
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    volumes:
      - pgadmin_dev_data:/var/lib/pgadmin
    networks:
      - app-network-dev
    depends_on:
      postgres:
        condition: service_healthy

  # huffaz-api:
  #   build:
  #     context: ../..
  #     dockerfile: apps/huffaz/api/Dockerfile.dev
  #   container_name: huffaz-api-dev
  #   restart: unless-stopped
  #   environment:
  #     NODE_ENV: development
  #     SERVER_PORT: 8001
  #     DB_HOST: postgres
  #     DB_PORT: 5432
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     HUFFAZ_DB_NAME: huffaz_db_dev
  #     JWT_SECRET: dev-jwt-secret
  #     JWT_EXPIRES_IN: 1h
  #     JWT_REFRESH_SECRET: dev-refresh-secret
  #     JWT_REFRESH_EXPIRES_IN: 7d
  #   ports:
  #     - "8001:8001"
  #   volumes:
  #     - ../../packages/database:/app/packages/database
  #     - ../../apps/huffaz/api:/app/apps/huffaz/api
  #   networks:
  #     - app-network-dev
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   command: pnpm run start:dev

  secondary-api:
    build:
      context: ../..
      dockerfile: apps/secondary/api/Dockerfile.dev
    container_name: secondary-api-dev
    restart: unless-stopped
    env_file:
      - ../../.env
    environment:
      NODE_ENV: development
      SERVER_PORT: 8002
      DB_HOST: postgres
      DB_PORT: 5432
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      SECONDARY_DB_NAME: secondary_db_dev
      JWT_SECRET: dev-jwt-secret
      JWT_EXPIRES_IN: 1h
      JWT_REFRESH_SECRET: dev-refresh-secret
      JWT_REFRESH_EXPIRES_IN: 7d
    ports:
      - "8002:8002"
    volumes:
      - ../../packages/database:/app/packages/database
      - ../../apps/secondary/api:/app/apps/secondary/api
      - secondary-api-node_modules:/app/apps/secondary/api/node_modules
      - secondary-database-node_modules:/app/packages/database/node_modules
    networks:
      - app-network-dev
    depends_on:
      postgres:
        condition: service_healthy
    command: sh -c "cd /app && pnpm install && pnpm --filter @arabiaaislamia/database run build && pnpm --filter @arabiaaislamia/storage run build && cd apps/secondary/api && pnpm run start:dev"

  # --- Frontend (UI) containers ---
  # main-website:
  #   build:
  #     context: ../..
  #     dockerfile: apps/main-website/Dockerfile.dev
  #   container_name: main-website-dev
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ../../apps/main-website:/app/apps/main-website
  #     - main-website-node_modules:/app/apps/main-website/node_modules
  #   networks:
  #     - app-network-dev
  #   command: sh -c "cd /app && pnpm install && cd apps/main-website && pnpm run dev"

  # huffaz-app:
  #   build:
  #     context: ../..
  #     dockerfile: apps/huffaz/client/Dockerfile.dev
  #   container_name: huffaz-app-dev
  #   restart: unless-stopped
  #   ports:
  #     - "3001:3001"
  #   volumes:
  #     - ../../apps/huffaz/client:/app/apps/huffaz/client
  #     - huffaz-app-node_modules:/app/apps/huffaz/client/node_modules
  #   networks:
  #     - app-network-dev
  #   command: sh -c "cd /app && pnpm install && cd apps/huffaz/client && pnpm run dev"

  secondary-app:
    build:
      context: ../..
      dockerfile: apps/secondary/client/Dockerfile.dev
    container_name: secondary-app-dev
    restart: unless-stopped
    ports:
      - "3011:3011"
    volumes:
      - ../../apps/secondary/client:/app/apps/secondary/client
      - secondary-app-node_modules:/app/apps/secondary/client/node_modules
    networks:
      - app-network-dev
    command: sh -c "cd /app && pnpm install && cd apps/secondary/client && pnpm run dev"

networks:
  app-network-dev:
    driver: bridge

volumes:
  postgres_dev_data:
    driver: local
  pgadmin_dev_data:
    driver: local
  secondary-api-node_modules:
    driver: local
  secondary-app-node_modules:
    driver: local
  secondary-database-node_modules:
    driver: local
  huffaz-app-node_modules:
    driver: local
  main-website-node_modules:
    driver: local
