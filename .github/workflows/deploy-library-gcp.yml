name: Deploy to Google Cloud VM

on:
  workflow_run:
    workflows: ["Build Library Docker Images"]
    types:
      - completed
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging
      image-tag:
        description: 'Docker image tag to deploy (defaults to latest)'
        required: false
        type: string

env:
  REGISTRY: ghcr.io
  DEPLOY_DIR: library-application

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Lowercase image name
        run: |
          echo "IMAGE_NAME_LOWER=${GITHUB_REPOSITORY,,}" >> $GITHUB_ENV

      # ─────────────────────────────────────────────
      # Prepare directory on GOOGLE CLOUD instance
      # ─────────────────────────────────────────────
      - name: Prepare deployment directory on GCP
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.GCP_HOST }}
          username: ${{ secrets.GCP_USERNAME }}
          key: ${{ secrets.GCP_SSH_KEY }}
          port: 22
          timeout: 20m 
          command_timeout: 20m
          script: |
            sudo mkdir -p /home/${{ secrets.GCP_USERNAME }}/${{ env.DEPLOY_DIR }}
            sudo chown -R ${{ secrets.GCP_USERNAME }}:${{ secrets.GCP_USERNAME }} /home/${{ secrets.GCP_USERNAME }}/${{ env.DEPLOY_DIR }}
            chmod -R 755 /home/${{ secrets.GCP_USERNAME }}/${{ env.DEPLOY_DIR }}

      # ─────────────────────────────────────────────
      # Copy compose + env files
      # ─────────────────────────────────────────────
      - name: Copy Docker Compose and config
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.GCP_HOST }}
          username: ${{ secrets.GCP_USERNAME }}
          key: ${{ secrets.GCP_SSH_KEY }}
          source: "infra/library/prod/docker-compose.yml,infra/library/prod/init-databases.sql,infra/library/prod/.env,infra/library/prod/config/Caddyfile"
          target: "/home/${{ secrets.GCP_USERNAME }}/${{ env.DEPLOY_DIR }}"
          strip_components: 2

      # ─────────────────────────────────────────────
      # Deploy on Google Cloud VM
      # ─────────────────────────────────────────────
      - name: Deploy application on GCP
        uses: appleboy/ssh-action@v1.0.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          host: ${{ secrets.GCP_HOST }}
          username: ${{ secrets.GCP_USERNAME }}
          key: ${{ secrets.GCP_SSH_KEY }}
          port: 22
          timeout: 20m
          command_timeout: 20m
          envs: REGISTRY,IMAGE_NAME_LOWER,GITHUB_TOKEN
          script: |
            set -e
            cd /home/${{ secrets.GCP_USERNAME }}/${{ env.DEPLOY_DIR }}

            echo "$GITHUB_TOKEN" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            IMAGE_TAG="${{ github.event.inputs.image-tag || github.event.workflow_run.head_sha || 'latest' }}"
            IMAGE_URL_API="$REGISTRY/$IMAGE_NAME_LOWER-library-api:$IMAGE_TAG"
            IMAGE_URL_APP="$REGISTRY/$IMAGE_NAME_LOWER-library-app:$IMAGE_TAG"

            echo "📥 Pulling Docker image: $IMAGE_URL_API"
            docker pull "$IMAGE_URL_API"
            echo "📥 Pulling Docker image: $IMAGE_URL_APP"
            docker pull "$IMAGE_URL_APP"
            echo "🛑 Stopping containers"
            docker compose -f docker-compose.prod.yml down --remove-orphans

            echo "♻️ Starting new version"
            docker compose -f docker-compose.prod.yml up -d --remove-orphans

            echo "🧹 Cleanup"
            docker image prune -f
            docker logout ghcr.io

            echo "✅ Deployment to GCP completed"

            
            